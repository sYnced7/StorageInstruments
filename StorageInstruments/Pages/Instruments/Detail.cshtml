@page "{instrumentId:int}"
@model StorageInstruments.Pages.Instruments.DetailModel
@{
    ViewData["Title"] = "Detail";
}

<h1>@Model.Instrument.Name</h1>

Very Soon

<a asp-page="./List" class="btn btn-default">Back</a>

@if(Model.Message != null)
{
    <div class="alert alert-info">@Model.Message</div>
}

